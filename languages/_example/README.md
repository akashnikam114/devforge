# Example Language Template

Copy this folder when adding support for a new coding language or framework.

Replace `_example` with a clear lowercase name such as:

- `node`
- `react`
- `python`
- `django`
- `spring-boot`
- `laravel`

Put the generated project files inside `templates/`.

